# Examples

The notebooks are the intended entry points:

- `synthetic/`: synthetic data with known object and probe truth.
- `chip/`: real chip data with subpixel-shifted probes.

Each notebook imports reusable code from `src/`.
