# Assets

This folder stores small assets used by the synthetic examples:

- `baboon.tiff`
- `cameraman.tif`
- `recon_zoneplate_Yi.mat`

Large real-data files should live in `assets/ptycho_real_data/` locally. They
are ignored by git.
